<section>
    <div class="row">
        <div class="small-12 columns">
            Ooops an the page you are visiting no longer exits.

            {{#each flashMessages.queue as |flash|}}
                {{flash-message flash=flash messageStyle='foundation'}}
            {{/each}}

        </div>
    </div>
</section>